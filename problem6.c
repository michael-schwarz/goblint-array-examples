int main(int argc, char const *argv[])
{
    int a[256];
    int i = 0;
    int zPend = 0;
    int ll_i;
    int nblock;

    while (i < nblock)
    { // TODO: Here
        if (a[0] == ll_i) // this needs to be var == var for the problem to occur
        {
            zPend++;
        }
        else
        {
            a[0] = 8;
        }
        
        i++;
    }

    return 0;  
}
